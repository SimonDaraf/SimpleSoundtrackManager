<UserControl x:Class="SimpleSoundtrackManager.MVVM.View.SessionSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleSoundtrackManager.MVVM.View"
             mc:Ignorable="d" 
             Height="50" d:DesignWidth="460">
    <Grid>
        <Button Background="{DynamicResource Background}" BorderBrush="{StaticResource DropdownBorder}" 
                BorderThickness="0, 0, 0, 1" Style="{StaticResource SessionButtonStye}"
                Command="{Binding NavigateToSessionCommand}">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Margin="5">
                    <Image Source="/Images/Icons/logo.png"/>
                    <TextBlock Text="{Binding Session.Name}" VerticalAlignment="Center" Margin="10, 0" Foreground="{StaticResource Foreground}" FontSize="14"/>
                </StackPanel>

                <TextBlock Grid.Column="2" Text="{Binding Session.LastModified}" Foreground="{StaticResource Foreground}" VerticalAlignment="Center" Margin="5, 0"/>
            </Grid>
        </Button>
    </Grid>
</UserControl>
