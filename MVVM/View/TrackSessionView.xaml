<UserControl x:Class="SimpleSoundtrackManager.MVVM.View.TrackSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleSoundtrackManager.MVVM.View"
             xmlns:vm="clr-namespace:SimpleSoundtrackManager.MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance Type=vm:TrackSessionViewModel}">
    <Border Background="{Binding TrackGradientBrush}" BorderBrush="{Binding Track.TrackColor.BrushColor}" BorderThickness="0, 0, 0, 1" Margin="10"
        Height="200">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="1" Margin="10, 0" Text="{Binding Track.Name}" Foreground="{StaticResource Foreground}"/>
        </Grid>
    </Border>
</UserControl>
