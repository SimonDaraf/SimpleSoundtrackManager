<UserControl x:Class="SimpleSoundtrackManager.MVVM.View.TrackEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleSoundtrackManager.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1" BorderBrush="{StaticResource DropdownBorder}"
                Padding="0, 10, 0, 0"
                BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Style="{StaticResource ButtonStyle}" Content="Back" Padding="10, 0" Height="25"
                        Command="{Binding BackCommand}"/>
                <Button Grid.Column="2" Style="{StaticResource ButtonStyle}" Content="Delete" Padding="10, 0" Height="25"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
